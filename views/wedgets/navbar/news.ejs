<a class="nav-link dropdown-toggle" href="#" id="newsDropdown" role="button" data-bs-toggle="dropdown"
    data-bs-auto-close="outside" aria-expanded="false">
    通知
    <span
        class="position-absolute translate-middle badge rounded-pill bg-danger news-count <%= newsList.numOfUnconfirm ? 'my-show' : 'my-noshow' %> ">
        <%= newsList.numOfUnconfirm %>
    </span>
</a>
<!-- 通知列表 -->
<ul class="dropdown-menu" aria-labelledby="navbarDropdown" id="newList">

    <!-- 新通知標題 -->
    <li class="dropdown-item <%= newsList.unconfirm.length ? 'my-show' : 'my-noshow' %>" id="unconfirm-news-title">新通知</li>

    <% if (newsList.unconfirm.length) { %>

    <%- include('./news-forEach', { list: newsList.unconfirm })  %> 

    <% } %>

    <!-- 先前通知的標頭 -->
    <li class="dropdown-item <%= newsList.confirm.length ? 'my-show' : 'my-noshow' %>" id="confirm-news-title">先前的通知</li>

    <% if (newsList.confirm.length) { %>

    <%- include('./news-forEach', { list: newsList.confirm })  %> 

    <% } %>
    
    <!-- 讀取更多 -->
    <li class="<%= newsList.total - newsList.limit >= 0  ? 'my-show' : 'my-noshow' %>" id="readMore">
        <button class="dropdown-item link-primary" type="button">
            讀取更多
        </button>
    </li>

    <!-- 沒有任何通知-->
    <li class="dropdown-item <%=  newsList.total - newsList.limit < 0 ? 'my-show' : 'my-noshow' %>" id="noNews">
        <span>沒有新通知</span>
    </li>

    <li>
        <hr class="dropdown-divider">
    </li>
</ul>