<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= albumList.author %></title>
</head>

<body>
    <ul>
        <% albumList.list.forEach( ({ id, title }) => { %>
        <li>
            <a href="/album/<%= id %>"><%= title %></a>
        </li>
        <% }) %>
    </ul>
</body>

</html>