<div style="display: none;">
  <%
      let whiteList = Object.value({
        user: ['currentUser', "relationShip", 'blogs'],
        blog: ['blog'],
        album: ['album'],
      }).flat()
  %>
  <% for(let type in locals){ %>
  <% if( whiteList.some(item => item === type)){ %>
  <div data-my-data="<%= type %>">
    <%- JSON.stringify(locals[type]) %>
  </div>
  <% } %>
  <% } %>
</div>